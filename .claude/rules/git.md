# Git

Never create commits unless the user explicitly asks for it.

Never push to a remote unless the user explicitly asks for it.  This applies
to all push variants (`git push`, force-push, push to a new branch, etc.) —
the user must opt in each time, even if a commit was just made at their
request.
